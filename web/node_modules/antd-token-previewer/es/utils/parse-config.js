import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import _slicedToArray from "@babel/runtime/helpers/esm/slicedToArray";
var _excluded = ["algorithm"],
  _excluded2 = ["algorithm"];
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
import { theme } from 'antd';
export var algorithmMap = {
  dark: theme.darkAlgorithm,
  compact: theme.compactAlgorithm
};
export var getAlgorithmString = function getAlgorithmString(algorithm) {
  var _Object$entries$find;
  return (_Object$entries$find = Object.entries(algorithmMap).find(function (_ref) {
    var _ref2 = _slicedToArray(_ref, 2),
      value = _ref2[1];
    return value === algorithm;
  })) === null || _Object$entries$find === void 0 ? void 0 : _Object$entries$find[0];
};
export var parsePlainConfig = function parsePlainConfig(config) {
  var algorithm = config.algorithm,
    rest = _objectWithoutProperties(config, _excluded);
  if (!algorithm) return rest;
  var parsedAlgorithms = Array.isArray(algorithm) ? algorithm.map(getAlgorithmString) : getAlgorithmString(algorithm);
  return _objectSpread(_objectSpread({}, rest), {}, {
    algorithm: parsedAlgorithms
  });
};
export var parseThemeConfig = function parseThemeConfig(config) {
  var algorithm = config.algorithm,
    rest = _objectWithoutProperties(config, _excluded2);
  if (!algorithm) return rest;
  var parsedAlgorithms = Array.isArray(algorithm) ? algorithm.map(function (item) {
    return algorithmMap[item];
  }).filter(Boolean) : algorithmMap[algorithm];
  return _objectSpread(_objectSpread({}, rest), {}, {
    algorithm: parsedAlgorithms
  });
};