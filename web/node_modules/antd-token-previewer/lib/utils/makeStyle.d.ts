import type { CSSInterpolation } from '@ant-design/cssinjs';
import type { GlobalToken } from 'antd/es/theme/interface';
import type React from 'react';
export type ThemeEditorToken = GlobalToken & {
    rootCls: string;
    componentCls: string;
    headerHeight: number;
};
declare const makeStyle: (path: string, styleFn: (token: ThemeEditorToken) => CSSInterpolation) => (prefixCls?: string) => [(node: React.ReactNode) => React.ReactElement, string];
export default makeStyle;
