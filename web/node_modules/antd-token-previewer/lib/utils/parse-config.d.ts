import type { ThemeConfig } from 'antd';
import { theme } from 'antd';
export type Algorithm = 'dark' | 'compact';
export interface PlainThemeConfig extends Omit<ThemeConfig, 'algorithm'> {
    algorithm?: Algorithm | Algorithm[];
}
export declare const algorithmMap: {
    [key in Algorithm]: typeof theme.darkAlgorithm;
};
export declare const getAlgorithmString: (algorithm: typeof theme.darkAlgorithm) => Algorithm;
export declare const parsePlainConfig: (config: ThemeConfig) => PlainThemeConfig;
export declare const parseThemeConfig: (config: PlainThemeConfig) => ThemeConfig;
