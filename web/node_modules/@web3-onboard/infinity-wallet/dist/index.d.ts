import type { WalletInit } from '@web3-onboard/common';
interface InfinityWalletOptions {
    chainId?: number;
}
declare function infinityWallet(options?: InfinityWalletOptions): WalletInit;
export default infinityWallet;
